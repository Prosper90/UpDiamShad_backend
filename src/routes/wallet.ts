// TODO: This route needs to be updated to work with new abstractWallet structure
// Temporarily disabled to fix build issues
import { Router } from 'express';

const router = Router();

// TODO: All wallet routes temporarily disabled until updated for abstractWallet structure
// Uncomment and fix when implementing multi-wallet support

export default router;